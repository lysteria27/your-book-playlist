# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Emotion model
transformers>=4.40.0
torch>=2.3.0
numpy>=1.26.0

# Spotify
# (uses httpx, no extra dependency needed)

# Dev / testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
